<div class="grid-row">
  <div class="column-two-thirds">
    <h1 class="heading-large"><%=t '.heading' %></h1>

    <p class="lede"><%=t '.lead_text_html', user_email: @user_email %></p>

    <!--
      TODO: Revisit this to use a helper method to say
      "Your case will be deleted in #{N} days"
      where N counts down from the case creation date.
    -->
    <p><%=t('.expire_warning', expire_in_days: Rails.configuration.x.cases.expire_in_days) %></p>

    <p><%=t '.return_info' %></p>

    <p><%= link_to t('.sign_in'), user_session_path %></p>
  </div>
</div>
