<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h3>The GLiMR Record  Generation Complete</h3>

    <p><%= @status.total - @status.failures %> / <%= @status.total %> successful.</p>
    <% if @status.failures > 0 %>
      <p>You need to return to the generation form to generate <%= @status.failures %> additional GLiMR <%= "record".pluralize(@status.failures) %>.</p>
    <% end %>
  </body>
</html>
